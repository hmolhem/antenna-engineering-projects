# Shared Assets

This directory is reserved for repository-level images, diagrams, and media used across multiple project pages.

Project-specific figures should remain inside the corresponding `projects/<project-name>/figures/` directory so that technical evidence stays close to its project record.
