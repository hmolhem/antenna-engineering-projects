# Native Model Access

Native CST binary models were not included in the supplied project archive used to
prepare this public package. The package therefore provides geometry records, figures,
analytical code, and a run manifest, but it cannot reproduce the original solver state
bit-for-bit.

A future release may add native models through Git LFS, a GitHub Release, or another
documented large-file mechanism after licensing and file-size review.
