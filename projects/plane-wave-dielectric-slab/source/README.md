# Plane-Wave Dielectric-Slab Portfolio Report Source

Compile from this directory with:

```text
pdflatex main.tex
biber main
pdflatex main.tex
pdflatex main.tex
```

The report uses the public `portfolio-report-template` architecture and the same
restrained academic visual language as the previously published antenna-engineering
portfolio reports.

The source package includes preserved CST screenshots, regenerated analytical figures,
verification code, captured console output, and a simulation run manifest.
