# Native Model Access

Native CST project files are not stored in the public repository. CST binaries are large, tool-version-dependent, and can contain extensive intermediate solver data.

The public package provides the report, complete LaTeX source, geometry and parameter descriptions, analytical verification code, selected CST result exports, and explicit numerical limitations. A reviewed native-model distribution can be prepared separately when a collaborator requires direct CST access.
